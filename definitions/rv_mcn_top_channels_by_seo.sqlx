config {
  type: "view",
  schema: "reporting",
  name: "rv_mcn_top_channels_by_seo",
  description: "Top Channels by SEO Score - For MCN Overview Dashboard",
  tags: ["reporting", "mcn", "channels", "seo", "dashboard"]
}

/*
 * Top Channels by SEO Score View
 * 
 * Purpose: Returns top channels ranked by average SEO score
 * 
 * Source: silver.silver_video, silver.silver_channel
 */

WITH latest_snapshot AS (
  SELECT MAX(snapshot_date) AS max_date
  FROM ${ref("silver", "silver_video")}
),
channel_seo AS (
  SELECT 
    sv.channel_id,
    sc.channel_title,
    sc.mcn_group_id,
    AVG(sv.seo_score) AS avg_seo_score,
    COUNT(sv.video_id) AS video_count
  FROM ${ref("silver", "silver_video")} sv
  JOIN ${ref("silver", "silver_channel")} sc
    ON sv.channel_id = sc.channel_id
    AND sv.snapshot_date = sc.snapshot_date
  CROSS JOIN latest_snapshot ls
  WHERE sv.snapshot_date = ls.max_date
  GROUP BY sv.channel_id, sc.channel_title, sc.mcn_group_id
)
SELECT * FROM channel_seo
ORDER BY avg_seo_score DESC
LIMIT 10

